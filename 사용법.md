# 도치의 아카이브 — 사용법

자료가 생기면 여기만 보시면 됩니다.

**모든 목록의 원본은 `data/items.json` 한 장입니다.**
여기만 고치면 [아카이브]와 [진학 자료실] 둘 다 자동으로 바뀝니다. 따로 만들 것이 없습니다.

[아카이브]: https://thecozyforest.github.io/dochis-archive/
[진학 자료실]: https://thecozyforest.github.io/dochis-warehouse/for-teachers/

---

## 방법 1 — 인터넷만으로 (터미널 안 씁니다)

### 1) HTML 파일을 공개 링크로 만들기

파일이 없고 링크만 있다면 이 단계는 건너뛰세요.

1. [창고 업로드 화면](https://github.com/thecozyforest/dochis-warehouse/upload/main)을 엽니다
2. 위쪽 파일 이름 칸에 **`폴더이름/`** 처럼 영문 폴더 이름을 치고 `/`를 누르면 폴더가 만들어집니다
3. HTML 파일을 끌어다 놓고 **Commit changes**
4. 주소는 `https://thecozyforest.github.io/dochis-warehouse/폴더이름/` 이 됩니다

> ⚠ **이미 있는 폴더 이름은 쓰지 마세요.** 기존 자료가 덮어써지고 링크트리에 걸린 주소가 깨집니다.

### 2) 등록 도우미로 내용 만들기

👉 **https://thecozyforest.github.io/dochis-archive/add.html**

빈칸을 채우면 붙여넣을 내용이 자동으로 만들어집니다. JSON을 직접 쓸 필요가 없습니다.

### 3) items.json에 붙여넣기

1. 도우미의 **복사하기** → **GitHub에서 items.json 열기**
2. `"items": [` 를 찾아 **바로 다음 줄**에 붙여넣기
3. **Commit changes**

1~2분 뒤 두 사이트에 함께 반영됩니다.

---

## 방법 2 — 컴퓨터에서 (파일이 여러 개일 때 편합니다)

터미널을 열고 먼저:

```bash
cd "C:\Users\기경민\Desktop\클로드 작업 그룹\도치의 아카이브\dochis-archive"
```

```bash
python tools/add.py "C:/경로/자료.html"
```

물어보는 대로 답하면 창고 업로드부터 등록까지 한 번에 됩니다.
파일 대신 **폴더**를 주면 안에 있는 것을 하나씩 물어봅니다.
`--dry-run`을 붙이면 미리 보기만 하고 아무것도 안 건드립니다.

끝나면 화면에 뜨는 `git` 명령 두 줄을 실행하시면 올라갑니다.

---

## "최종본이 뭐였지?" — 폴더 정리하기

```bash
python tools/tidy.py "C:/경로/폴더"
```

같은 자료의 여러 버전을 묶어 **어느 게 최신인지** 알려 줍니다.
`_최종`, `v5b`, `(3)` 같은 이름은 순서를 알려주지 못하므로 **파일 수정시각**으로 판단합니다.

구버전을 치우려면 `--apply`를 붙이세요. **지우지 않고** `_구버전/` 폴더로 옮기기만 합니다.

### 자동으로 안 치우는 경우

이름에 `정리본`·`배포용`·`소장용` 같은 **용도 표시**가 서로 다르거나, 크기가 10배 넘게 벌어지면
구버전이 아니라 **용도가 다른 파일**로 보고 그대로 둡니다.

> 실제 예: `(정리본) KU_세학생_생기부_복기본` 56KB 와 `KU_세학생_생기부_복기본_v1(2)_맥락보완` 12MB
> — 앞은 공개용, 뒤는 소장용. 둘 다 남겨야 맞습니다.

### 앞으로 안 헷갈리려면

`_최종`, `_진짜최종` 대신 **날짜**를 쓰세요. 이름만 봐도 순서가 보입니다.

```
스튜디오시선_교사제출용_20260418.docx
스튜디오시선_교사제출용_20260729.docx   ← 이게 최신
```

용도가 다르면 용도를 적어 두세요. `(정리본)`, `(소장용)`.

---

## 항목 하나가 어떻게 생겼나

```jsonc
{
  "id": "csat-checker",          // 겹치지 않는 이름. 창고 폴더 이름을 쓰면 편합니다
  "name": "2027 수능최저 조회기",
  "desc": "한 줄 설명",
  "type": "app",                 // app 앱·도구 | doc 문서 | form 폼 | lecture 연수 | book 저서
  "tags": ["진학", "상담"],
  "date": "2026-06-01",          // 모르면 null — 캘린더에 안 뜨고 목록에는 나옵니다
  "dateNote": "리포 등록일",
  "channel": "public",           // public 공개 | dorms 교사인증 | private 비공개
  "pack": true,                  // 진학 자료실에 실을지
  "href": "https://..."          // 비공개면 넣지 않습니다
}
```

**`channel`이 정하는 것**

| | 아카이브 | 진학 자료실 |
|---|---|---|
| `public` | 링크와 함께 | `pack: true`면 링크와 함께 |
| `dorms` | 링크와 함께 | **이름만** (URL은 안 나감) |
| `private` | 토글을 켜야 보임, 링크 없음 | 안 나감 |

**`pack`이 정하는 것** — 진학 자료실 수록 여부입니다.
진학·생기부 자료만 `true`로 하세요. 학급운영·수업 자료는 `false` — 도치의 창고가 담당합니다.

---

## 주소

| | |
|---|---|
| 아카이브 (전체 추적용) | https://thecozyforest.github.io/dochis-archive/ |
| 등록 도우미 | https://thecozyforest.github.io/dochis-archive/add.html |
| 진학 자료실 (진학방 배포용) | https://thecozyforest.github.io/dochis-warehouse/for-teachers/ |
| 도치의 창고 (학급운영·수업) | https://thecozyforest.github.io/dochis-warehouse/ |

---

## 하지 말 것

- **링크트리(`cozyforest` 리포)는 건드리지 않습니다.** 별개 프로젝트입니다.
- **도치의 창고 기존 파일**(`index.html`, `warehouse-index-base.html`, 기존 앱 폴더)도 건드리지 않습니다.
  링크트리가 그 주소들을 가리키고 있어서, 폴더 이름을 바꾸면 링크가 깨집니다.
