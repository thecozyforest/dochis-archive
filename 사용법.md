# 도치의 아카이브 — 사용법

앞으로 자료가 생기면 여기만 보시면 됩니다.

모든 명령은 **`dochis-archive` 폴더 안에서** 실행합니다. 터미널을 열고 먼저 이것부터:

```bash
cd "C:\Users\기경민\Desktop\클로드 작업 그룹\도치의 아카이브\dochis-archive"
```

---

## 1. 자료 하나 추가하기

```bash
python tools/add.py "C:/경로/자료.html"
```

물어보는 대로 답하면 끝입니다.

- **이름 / 한 줄 설명** — 사람들에게 보일 제목과 설명
- **어떤 자료인가요** — 앱·도구 / 문서·자료 / 폼·템플릿 / 연수·강의 / 저서
- **태그** — 쉼표로 구분 (`진학,학종,생기부`)
- **누가 볼 수 있나요**
  - `공개` — 누구나 열람
  - `DoRms 한정` — 자료실에는 **이름만** 나가고 링크는 안 나갑니다
  - `비공개` — 링크를 아예 넣지 않습니다
- **진학 자료실에 실을까요** — 진학·생기부 자료만 `y`. 학급운영·수업은 `n`(도치의 창고가 담당)
- **창고에 올려서 공개 링크를 만들까요** — `y`를 누르면 HTML을 도치의 창고에 넣고 주소를 만들어 줍니다

끝나면 화면에 뜨는 `git` 명령 두 줄을 복사해 실행하시면 인터넷에 올라갑니다.

**여러 개를 한 번에** 넣으려면 파일 대신 폴더를 주세요. 안에 있는 파일을 하나씩 물어봅니다.

```bash
python tools/add.py "C:/경로/폴더"
```

**미리 보기만** 하려면 뒤에 `--dry-run`을 붙이세요. 아무것도 안 건드립니다.

---

## 2. "최종본이 뭐였지?" — 폴더 정리하기

```bash
python tools/tidy.py "C:/경로/폴더"
```

같은 자료의 여러 버전을 묶어서 **어느 게 최신인지** 알려 줍니다.
`_최종`, `v5b`, `(3)` 같은 이름은 순서를 알려주지 못하므로 **파일 수정시각**으로 판단합니다.

구버전을 치우려면:

```bash
python tools/tidy.py "C:/경로/폴더" --apply
```

**지우지 않습니다.** `_구버전/` 폴더로 옮기기만 합니다.

### 자동으로 안 치우는 경우

이름에 `정리본`·`배포용`·`소장용` 같은 **용도 표시**가 서로 다르거나, 파일 크기가 10배 넘게 벌어지면
구버전이 아니라 **용도가 다른 파일**로 보고 그대로 둡니다.

> 실제 예: `(정리본) KU_세학생_생기부_복기본` 56KB 와 `KU_세학생_생기부_복기본_v1(2)_맥락보완` 12MB
> — 앞은 공개용, 뒤는 소장용. 둘 다 남겨야 맞습니다.

### 앞으로 안 헷갈리려면

`_최종`, `_최종본`, `_진짜최종` 대신 **날짜**를 쓰세요.

```
스튜디오시선_교사제출용_20260418.docx
스튜디오시선_교사제출용_20260729.docx   ← 이름만 봐도 이게 최신
```

용도가 다른 파일은 용도를 적어 두세요. `(정리본)`, `(소장용)`.

---

## 3. 손으로 고치기

`dochis-archive/data/items.json` 을 열어 고치면 됩니다. 자주 고칠 것:

- **날짜 채우기** — `"date": null` 을 `"date": "2026-03-15"` 로 바꾸면 캘린더에 올라갑니다
- **설명 다듬기** — `desc`
- **공개 등급 바꾸기** — `channel`

고친 뒤에는 자료실도 갱신해 주세요.

```bash
python tools/build_public.py
```

---

## 4. 인터넷에 올리기

```bash
git add -A && git commit -m "자료 추가" && git push
```

```bash
cd ../dochis-warehouse && git add -A && git commit -m "자료 추가" && git push && cd ../dochis-archive
```

올리고 1~2분 뒤에 반영됩니다.

---

## 주소

| | |
|---|---|
| 아카이브 (전체 추적용) | https://thecozyforest.github.io/dochis-archive/ |
| 진학 자료실 (진학방 배포용) | https://thecozyforest.github.io/dochis-warehouse/for-teachers/ |
| 도치의 창고 (학급운영·수업) | https://thecozyforest.github.io/dochis-warehouse/ |

---

## 하지 말 것

- **링크트리(`cozyforest` 리포)는 건드리지 않습니다.** 별개 프로젝트입니다.
- **도치의 창고 기존 파일**(`index.html`, `warehouse-index-base.html`, 기존 앱 폴더)도 건드리지 않습니다.
  링크트리가 그 주소들을 가리키고 있어서, 폴더 이름을 바꾸면 링크가 깨집니다.
